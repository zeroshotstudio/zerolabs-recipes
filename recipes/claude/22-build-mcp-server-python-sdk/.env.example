# Environment configuration for MCP Python Server
# Server port (if running SSE transport mode)
MCP_PORT=8000
MCP_HOST=127.0.0.1

# Environment mode: development or production
APP_ENV=development

# Optional log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
